---
interface Props {
  href: string;
  label: string;
}
const { href, label } = Astro.props;
---

<a
  href={href}
  class="inline-block rounded bg-zinc-950 px-6 py-3 text-sm font-semibold uppercase tracking-wider text-zinc-50 transition hover:bg-zinc-950/90"
>
  {label}
</a>