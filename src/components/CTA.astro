---
interface Props {
  href: string;
  label: string;
}
const { href, label } = Astro.props;
---

<a
  href={href}
  class="inline-block rounded bg-curtain px-6 py-3 text-sm font-semibold uppercase tracking-wider text-paper transition hover:bg-curtain/90"
>
  {label}
</a>